@model Applicant
@{
    ViewBag.Title = "Part I";
}

<div class="row">
    <h1>Part I</h1>
    <p>
        Create a simple Applicant form, which inserts the following fields to the <i>[InterviewTest].[dbo].[Applicant]</i> table:
        <ul>
            <li>First name (required)</li>
            <li>Middle name</li>
            <li>Last name (required)</li>
            <li>Position Id (required)</li>
            <li>Comments</li>
            <li>Remote (required)</li>
        </ul>
    </p>
    <p>Implement front-end (javascript) <b>AND</b> back-end (server-side) validation on required fields. Only a valid form should be inserted into the database!</p>
    <p>Add your code to this view.</p>
    <hr/>

    <!-- Your code here -->
    @*@using (Html.BeginForm("PartOne", "Test", FormMethod.Post))*@
    @using (Html.BeginForm())
    {

        @Html.AntiForgeryToken()
        <p>
            @Html.TextBoxFor(m => m.firstName)
        </p>
        <p>
            @Html.TextBoxFor(m => m.middleName)
        </p>
        <p>
            @Html.TextBoxFor(m => m.lastName)
        </p>
        <p>
            @Html.TextBoxFor(m => m.positionId)
        </p>
        <p>
            @Html.TextAreaFor(m => m.comments)
        </p>
        <fieldset id="isRemote" name="isRemote">
            <p>
                Do you work remotely?
                @Html.RadioButtonFor(m => m.remote, true) Yes

                @Html.RadioButtonFor(m => m.remote, false) No
            </p>
        </fieldset>
        <p><input id="Submit" type="submit" value="submit"/></p>
    }


    @*<form id="applicantform" asp-controller="Test" asp-action="SaveFormData" method="post">
            <fieldset>
                <p><input type="text" id="firstname" name="firstname" placeholder="First Name" required /></p>
                <p><input type="text" id="middlename" name="middlename" placeholder="Middle Name" /></p>
                <p><input type="text" id="lastname" name="lastname" placeholder="Last Name" required /></p>
                <p><input type="text" id="positionid" name="positionid" placeholder="Position Id" required /></p>
                <p><textarea type="text" id="comments" name="comments" placeholder="Comments"></textarea></p>
                <fieldset id="isRemote" name="isRemote">
                   <p>Do you work remotely?
                    <input type="radio" id="remote" name="remote" value="1" required />
                    <label for="remote">Yes</label>

                    <input type="radio" id="remote" name="remote" value="0" required />
                    <label for="notremote">No</label>
                   </p>
                </fieldset>
                <input type="submit" id="submit" value="submit" name="submit"/>
            </fieldset>
        </form>*@

    @*<script>
            function Submit() {
                var applicantInfo = {
                    firstName: $('#firstname').val(),
                    middleName: $('#middlename').val(),
                    lastName: $('#lastname').val(),
                    positionID: $('#positionid').val(),
                    comments: $('#comments').val(),
                    remote: $('#remote').is(':checked')
                    //notremote: $('#notremote').is(':checked')
                };

                $.post("/Test/PartOne", { applicant: applicantInfo },
                    function () { alert('Posted.') });

        </script>*@

    @{


    }
</div>