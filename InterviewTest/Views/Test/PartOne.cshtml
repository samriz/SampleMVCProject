@model Applicant
@{
    ViewBag.Title = "Part I";
}

<div class="row">
    <h1>Part I</h1>
    <p>
        Create a simple Applicant form, which inserts the following fields to the <i>[InterviewTest].[dbo].[Applicant]</i> table:
        <ul>
            <li>First name (required)</li>
            <li>Middle name</li>
            <li>Last name (required)</li>
            <li>Position Id (required)</li>
            <li>Comments</li>
            <li>Remote (required)</li>
        </ul>
    </p>
    <p>Implement front-end (javascript) <b>AND</b> back-end (server-side) validation on required fields. Only a valid form should be inserted into the database!</p>
    <p>Add your code to this view.</p>
    <hr />

    <!-- Your code here -->
    @*@using (Html.BeginForm("PartOne", "Test", FormMethod.Post))*@
    @using (Html.BeginForm(new { id = "applicantform", name = "applicantform" }))
    {

        @Html.AntiForgeryToken()
        @Html.ValidationSummary(false, "", new { @class = "text-danger" })
        <p>
            @Html.TextBoxFor(m => m.firstName, new { @placeholder = "First Name" })
        </p>
        <p>
            @Html.TextBoxFor(m => m.middleName, new { @placeholder = "Middle Name" })
        </p>
        <p>
            @Html.TextBoxFor(m => m.lastName, new { @placeholder = "Last Name" })
        </p>
        <p>
            @Html.TextBoxFor(m => m.positionId, new { @placeholder = "Position Id" })
        </p>
        <p>
            @Html.TextAreaFor(m => m.comments, new { @placeholder = "Any Comments" })
        </p>
        <fieldset id="isRemote" name="isRemote">
            <p>
                Do you work remotely?
                @Html.RadioButtonFor(m => m.remote, true) Yes

                @Html.RadioButtonFor(m => m.remote, false) No
            </p>
        </fieldset>
        <p><input id="Submit" type="submit" value="submit" onclick="validate()" /></p>
    }

    @*<script>
            function validate()
            {
                var firstName = document.forms["applicantform"]["firstName"].value;
                if (firstName == "")
                {
                    alert("First name is required.");
                    return false;
                }
                var lastName = document.forms["applicantform"]["lastName"].value;
                if (lastName == "")
                {
                    alert("Last name is required.");
                    return false;
                }
                var positionId = document.forms["applicantform"]["positionId"].value;
                if (positionId == "") {
                    alert("Position Id is required.");
                    return false;
                }
                var remote = document.forms["applicantform"]["remote"].value;
                if (remote == "")
                {
                    alert("Remote status is required.");
                    return false;
                }
                alert("Form Submitted.");

                return true;
            }

        </script>*@
</div>